import React from 'react';

const UseState = (props) => {
    const [count, setCount] = usaState(0)|

    return (
        <div className="UseState">
            <PageTitle
                title="Hook UseState"
                subtitle="Estado em componentes funcionais!"
            />
        </div>
    )
}

export default UseState;
